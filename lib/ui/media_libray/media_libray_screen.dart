import 'package:flutter/cupertino.dart';

class MediaLibrayScreen extends StatelessWidget {
  const MediaLibrayScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SafeArea(child: Center(child: Text("Media Library Under development")));
  }
}
